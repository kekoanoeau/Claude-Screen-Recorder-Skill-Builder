// Stub kept for future import-time wiring — Phase 2 ships upload + plain
// JSON download (see background.js downloadRecording). When we add a
// proper zip exporter (jszip) it lands here and is imported by background.js.
//
// Not bundled into manifest.json yet — leaving this file as a placeholder
// so the directory layout matches the plan.
export {};
